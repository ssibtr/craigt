<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <template id="portal_template_save_cart" name="portal template save cart" inherit_id="sale.sale_order_portal_template">
              <xpath expr="//t[10]" position="replace">
                <t t-if="not sale_order.is_expired and sale_order.state in ['draft', 'sent', 'saved']">
                    <li t-if="sale_order.remaining_validity_days &gt; 0" class="list-group-item">
                        <small><b class="text-muted">This offer expires in</b></small>
                        <div class="day_counter">
                            <i class="fa fa-clock-o" aria-title="Dates" title="Dates"/>
                            <t t-esc="sale_order.remaining_validity_days"/>
                            <t t-if="sale_order.remaining_validity_days &gt; 1">days</t><t t-else="">day</t>
                        </div>
                    </li>
                    <li t-if="sale_order.amount_undiscounted &gt; sale_order.amount_untaxed" class="list-group-item flex-grow-1">
                        <small><b class="text-muted">Your advantage</b></small>
                        <small>
                            <b t-field="sale_order.amount_undiscounted" t-options="{&quot;widget&quot;: &quot;monetary&quot;, &quot;display_currency&quot;: sale_order.pricelist_id.currency_id}" style="text-decoration: line-through" class="d-block mt-1"/>
                        </small>
                        <t t-if="sale_order.amount_untaxed == sale_order.amount_total">
                            <h4 t-field="sale_order.amount_total" class="text-success"/>
                        </t>
                        <t t-else="">
                            <h4 t-field="sale_order.amount_untaxed" class="text-success mb-0"/>
                            <small>(<span t-field="sale_order.amount_total"/> Incl. tax)</small>
                        </t>
                    </li>
                </t>
              </xpath>
        </template>


        <template id="portal_reload" name="portal template save cart" inherit_id="sale.sale_order_portal_template">
              <!-- <xpath expr="//[hasclass('o_portal_approval')]" position="after"> -->
              <xpath expr="//[hasclass('mb-0')][1]" position="after">
                  <!--CUSTOM-->
                  <!--<input t-if="sale_order.partner_id.parent_id.x_studio_approval_role == 'admin'" onload="redirectToHome()" id="redirect_admin_secret_input" type="text"  t-att-value="sale_order.partner_id.parent_id.x_studio_approval_role" hidden="hidden"/>-->
                  <script t-if="sale_order.x_studio_approval">
                    window.location.href = '/my/home'
                  </script> 
              </xpath>
        </template>



    </data>
</odoo>

 
 
 
 
 
